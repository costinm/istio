# Single version
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: fortioistio
spec:
  hosts:
  - "fortio.v08.istio.webinf.info"
  gateways:
  - istio08-gateway
  http:
  - route:
    - destination:
        host: fortiov1.test.svc.cluster.local
        port:
          number: 8080
        subset: v1
      weight: 60
    - destination:
        host: fortiov1.test.svc.cluster.local
        port:
          number: 8080
        subset: v2
      weight: 30
    - destination:
        host: fortiov2.test.svc.cluster.local
        port:
          number: 8080
      weight: 10
